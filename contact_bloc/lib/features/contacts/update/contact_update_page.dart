import 'package:flutter/material.dart';

class ContactUpdatePage extends StatelessWidget {

  const ContactUpdatePage({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Contact Update'),),
           body: Container(),
       );
  }
}